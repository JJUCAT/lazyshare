gui 使用 pyside 实现：
1.src/gui/business 负责后端业务逻辑。
2.src/gui/ui 负责前端显示。
3.config/gui.json 配置文件：
  "tmp"：缓存数据保存路径，包括最近打开文件。


一.图表
1.左侧主窗口 chart_win
  a.显示多个纵向排列小窗口 sub_win，横轴是时间轴，且多个窗口对齐。
  b.sub_win 数据用“点线”显示。
  c.时间轴显示数据量 show_days 默认 21 日数据。
    右上角有“+”将 show_days 翻倍，“-”将 show_days 缩小一半。
  d.时间轴每列有隐形时间柱。
    鼠标点击时间柱，所有 sub_win 高亮相同时间柱，悬浮 float_win 显示具体时间和数值。
    鼠标执行其他动作关闭 float_win。
  e.横轴有滚动条，默认在最右侧，也就是默认窗口显示最新数据。
  f.纵轴没有滚动条，左侧有 5 个刻度，右侧也有 5 个刻度，自适应显示刻度值。
  g.sub_win 左上角显示子窗口名，曲线数据源。
2.右侧副窗口 info_win
  a.标题从 csv 文件列项读取显示股票名称，代码。
二.菜单栏
1.文件（下拉）
  a.打开文件（弹窗选择文件）：加载 csv 文件，把显示内容打包进缓存路径中，方便下次快速显示。
  b.打开最近文件(右侧显示)：显示最近打开的文件，最多10个。
2.编辑（下拉）
  a.添加数据（弹窗）：选择列项数据，选择加载到 chart_win 中哪个 sub_win 或创建新窗口。
    选择数据加载到图表左纵列还是右纵列，纵列不显示单位。
    sub_win 用列项名命名，sub_win 有多个列项数据的话用列项名和“-”拼接命名。
    同个 sub_win 内，不同列项数据用不同颜色显示。
  b.删除数据（弹窗）：选择 sub_win 中要删除的数据源。
三.后端
1.缓存管理
  a.缓存路径在 json 的 tmp 变量路径。
  b.打开新文件时候，旧文件的显示内容例如 sub_win，show_days 这些打包缓存。
    注意不包含 csv 内的数据数值。
  c.缓存最多 100 个文件的数据。
  d.打开文件（或最近文件）：先从缓存中找记录，有就快速打开。

